interface:
  display_name: "Release CodeSpec"
  short_description: "Audit, prepare, publish, and finalize releases"
  default_prompt: "Use $release-codespec to audit the current release state and take the next safe release step."
