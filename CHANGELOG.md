# CodeSpec

## 1.0.0

### 首次发布

- 发布包名为 `@hrhy-ai/codespec`，命令行为 `codespec`。
- 工作区统一使用 `codespec/`；当前规格存放在 `codespec/specs/`，已归档 Change 存放在 `codespec/archive/changes/`。
- 对外只提供 `codespec-workflow`、`codespec-rebase-change`、`codespec-archive-change` 三个 Agent Skill。
- 为 Change 引入 SDD 分级、归档影响分析、可验证的归档回归证据，以及可追溯的归档历史记录。
